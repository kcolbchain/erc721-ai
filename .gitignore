node_modules/
cache/
out/
broadcast/
sdk/typescript/dist/
__pycache__/
*.pyc
*.pyo
.env
.venv/
venv/

# Foundry deps are installed in CI via `forge install --no-git`; keep local install artifacts out of git
lib/
foundry.lock
.gitmodules
